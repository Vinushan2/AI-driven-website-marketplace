// vite.config.js
import { defineConfig } from 'vite';
// import tailwind-related plugins

export default defineConfig({
  base: 'https://easydigi.shop/',
  assetsInclude: ['**/*.webp'],
  plugins: [
    // Remove any Tailwind-related plugins
    // tailwind-related plugins can be removed
  ],
});
